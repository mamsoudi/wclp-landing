+sprite(id, path, viewBox, classes)
  - var viewBox = viewBox || "0 0 1 1"
  span(class=`sprite -${id} ${classes}`)
    svg(viewBox=`${viewBox}`)
      use(xlink:href=`${path}images/icons.svg#${id}`)
